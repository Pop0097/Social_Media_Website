<!-- Used to display form required to display comment -->
<h4> Post a comment </h4>

<%= form_with(model: [@post, @post.comments.build], local: true) do |form| %>
    <%= comment.commenter = @person %>
  <p>
    <%= form.text_area :body, placeholder: 'Enter Commment' %>
  </p>
  <p>
    <%= form.submit %>
  </p>
<% end %>
