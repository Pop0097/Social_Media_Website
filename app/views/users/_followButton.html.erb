<% if current_user.id != @user.id %>
  <% if current_user.following?(@user) %>
    <%= button_to 'Unfollow', follows_path(user_id: @user.id), method: :delete %>
  <% else %>
    <%= button_to 'Follow', follows_path(user_id: @user.id) %>
  <% end %>
<% end %>
